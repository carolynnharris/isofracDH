% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frac_single.R
\name{add_frac_single}
\alias{add_frac_single}
\title{Add single-component D/H fractionation (Sessions 2005, Section 3.1)}
\usage{
add_frac_single(df, source_d2H, alpha, name = "product_d2H")
}
\arguments{
\item{df}{A data.frame or tibble.}

\item{source_d2H}{Column containing source \eqn{\delta D} values in per mille (‰).}

\item{alpha}{Fractionation factor (\eqn{\alpha}). Either a single numeric value
or a column in \code{df}.}

\item{name}{Name of the new output column to create. Default \code{"product_d2H"}.}
}
\value{
\code{df} with an added column containing the calculated product \eqn{\delta D}
in per mille (‰).
}
\description{
Calculates a product isotope composition column based on a single source
fractionated by fractionation factor (alpha), following Sessions (2005) Section 3.1.
}
\details{
\strong{Units:} Input \code{source_d2H} must be in per mille (‰). Internally, this
function converts \eqn{\delta} to fractional form (\eqn{\delta/1000}) when
applying the equation, then returns the result in per mille (‰).

\strong{Alpha:} \code{alpha} can be a single numeric value (used for all rows) or
a column in \code{df}.
}
\examples{
library(dplyr)

df <- tibble::tibble(source_d2H = seq(-300, 50, by = 50))

# alpha as a scalar
df |> add_frac_single(source_d2H = source_d2H, alpha = 0.92)

# alpha as a column
df2 <- df |> mutate(alpha = seq(0.90, 0.94, length.out = n()))
df2 |> add_frac_single(source_d2H = source_d2H, alpha = alpha, name = "delta_prod")

}
